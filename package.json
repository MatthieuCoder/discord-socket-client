{
  "name": "herobot-socket-handler",
  "version": "1.0.0",
  "description": "Some stuff to recieve events from the discord gateway.",
  "main": "exit",
  "author": "Matthieu",
  "license": "APACHE-2",
  "private": true,
  "dependencies": {
    "@types/ws": "^7.2.3",
    "pako": "^1.0.11",
    "ws": "^7.2.3",
    "zlib-sync": "^0.1.6"
  },
  "scripts": {
    "build": "tsc -p . || true && cd dist && node index"
  }
}
